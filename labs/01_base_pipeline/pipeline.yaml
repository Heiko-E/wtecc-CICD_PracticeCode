apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:  
  name: echo
spec:
  params:
    - name: message
      description: The message to echo
      type: string
  tasks:
    - name: echo-message
      image: alpine:3
      command: [/bin/echo]
      args: ["$(params.message)"]
